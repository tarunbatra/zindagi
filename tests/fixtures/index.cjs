const GliderStates = require('./glider-states.json');
const GliderStatesStitchedEdges = require('./glider-states-stitchedEdges.json');

module.exports = {
  GliderStates,
  GliderStatesStitchedEdges,
};